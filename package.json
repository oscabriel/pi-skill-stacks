{
  "name": "pi-skill-stacks",
  "version": "0.1.0",
  "description": "Pi package that groups skills into named stacks you can toggle on/off from a /stacks picker, keeping the system prompt small. Exclusions are written through pi's own settings override mechanism, and a compact [Skills] header line replaces the per-scope listing.",
  "keywords": ["pi-package", "pi", "extension", "skills", "context", "skill-stacks"],
  "license": "MIT",
  "type": "module",
  "files": ["extensions", "src", "README.md"],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "pi": {
    "extensions": ["./extensions/index.ts", "./extensions/header.ts"]
  }
}
